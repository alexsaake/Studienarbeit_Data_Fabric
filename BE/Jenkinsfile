pipeline {
    agent any

    stages {

        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }

        stage("Compilation") {
           steps {
                sh "mvn clean install"
            }
        }
    }
}